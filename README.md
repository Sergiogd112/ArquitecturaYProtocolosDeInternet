# ArquitecturaYProtocolosDeInternet

 Solver for IT excercises from the API subject in UPC.!! Use at you own discretion!!
 requires:
 ```
python=3.10^
poetry
```
to install them run:
```
sudo apt update
sudo apt install python3 python-pip
sudo pip install poetry
pip install poetry
```
